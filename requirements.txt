# Python deps for BitNet/TBN pretraining via HuggingFace Trainer.
# Install torch separately, matched to your CUDA. Stable releases are fine
# (no nightly required) — e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124

torch >= 2.4
transformers >= 4.45
accelerate >= 1.0
datasets >= 3.0
tokenizers >= 0.15
huggingface_hub >= 0.25

# Logging / monitoring
wandb
tensorboard

# Misc
einops
safetensors
